app.controller('LoginsCtrl', ['$scope', '$http', function($scope, $http){
  console.log("working");
  $scope.message = "working";

  var data = {
    user: {
      email: 'harry@chen.com',
      password: 'asdasdasd'
    }
  };

  var url = "<%= ENV['URL'] %>" || "http://localhost:3000/"

  $http.post(url + 'users/sign_in', data).success(function(response){
    console.log(response)

    $http.get(url + 'posts').success(function(response){
      console.log(response)
    });
  });
}]);